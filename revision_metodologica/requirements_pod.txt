# Dependencias de la revision metodologica.
#
# PyTorch se instala aparte (rueda CUDA, no esta en el indice por defecto):
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# En un pod con imagen de PyTorch ya viene instalado: no reinstalarlo.
#
# Version minima, no exacta: el pod trae su propio Python (a menudo mas viejo
# que el del portatil) y fijar version exacta rompe el pip install si esa rueda
# no existe para esa version de Python. Los resultados neuronales ya no son
# bit a bit reproducibles entre maquinas aunque coincida hasta el ultimo pin
# (no-determinismo de CUDA en coma flotante), asi que fijar la version exacta
# no compraba reproducibilidad real: solo compraba fragilidad de instalacion.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
geopandas>=0.14
libpysal>=4.9
mgwr>=2.2
scikit-gstat>=1.0
statsmodels>=0.14
matplotlib>=3.7
pymupdf>=1.23
pytest>=7.0
